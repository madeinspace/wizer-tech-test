Note: the following MUI component in the Accordeon was throwing some dependency errors

```bash
<ExpandMoreIcon />
```

install with the following:

```bash
 npm install @mui/icons-material --legacy-peer-deps
```
